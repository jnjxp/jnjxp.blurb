<?php

$edit = '';
if (isset($auth) && $auth->isAuthenticated()) {
    $url = $this->url('blurb.edit', ['blurb_id' => $blurb->getBlurbId()]);
    $pattern = '<a href="%s" class="admin-link admin-edit">Edit</a>';
    $edit = sprintf($pattern, $url);
}

?>

<div class="blurb">
    <?php echo $edit; ?>
    <?php echo $blurb->getContent(); ?>
</div>
